# Rocky 使用说明文档 — 版本变更记录

在线地址：https://yangtiansky.github.io/rocky-doc/

---

## v1 — 初始手风琴版
**文件：** `v1_accordion_purple.html`
**提交：** `4f09ce9` — Rocky 使用说明文档 - 展开式手风琴版

### 设计特征
- 浅紫渐变背景 `#faf5ff`
- 系统 sans-serif 字体
- 紫色主题 `#7c3aed`
- 手风琴折叠/展开交互（17 个区块）
- 卡片阴影 + 12px 大圆角
- 实心彩色标签 pill
- 全局「展开全部 / 折叠全部」按钮
- 代码块浅紫背景

---

## v2 — OpenCode 暗色终端风格
**文件：** `v2_opencode_dark.html`
**提交：** `523287a` — redesign: OpenCode terminal-native monospace-first aesthetic

### 设计变更
| 维度 | v1 → v2 |
|------|---------|
| 背景 | 浅紫 → 暖黑 `#201d1d` |
| 字体 | sans-serif → 全站等宽 Berkeley Mono |
| 配色 | 紫色单系 → Apple HIG 语义色 (蓝 `#007aff` 为主) |
| 阴影 | 有 → 完全扁平零阴影 |
| 圆角 | 12/20px → 统一 4px |
| 间距 | 随意 → 8px 基准网格 |
| 折叠图标 | `+` 旋转 → ASCII `[+]` / `[-]` |
| 列表 | 圆点 → ASCII `[+]` / `[x]` 前缀 |
| 标签 | 实心填充 → 细边框空心 pill |
| Hero | 纯文字标题 → ASCII 像素字标 + 38px 标题 |

---

## v3 — 紫色导航栏
**文件：** `v3_purple_nav.html`
**提交：** `5ac886f` — 重新设计：紫色主题 + 顶部导航栏

### 设计变更
| 维度 | v2 → v3 |
|------|---------|
| 主色 | 蓝 `#007aff` → 紫 `#7c3aed` |
| 导航栏 | 无 → 顶部新增 `Rocky | Git | 文档 | 下载` |
| 导航样式 | — | 等宽字体、4px 圆角、细线边框、零阴影 |
| 激活态 | — | 紫色边框 + 紫色半透明背景 |
| hover 态 | — | 灰色 → 紫色边框 |

---

## v4 — 平铺展开布局
**文件：** `v4_flat_layout.html`
**提交：** `85a8359` — 改为平铺展开布局，去掉手风琴折叠和全局按钮

### 设计变更
| 维度 | v3 → v4 |
|------|---------|
| 布局 | 手风琴折叠 `<div class="accordion">` 嵌套 4 层 | 平铺 `<div class="section">` 底部细线分隔 |
| 全局按钮 | 展开全部 / 折叠全部 | 移除 |
| JavaScript | toggleAccordion() / toggleAll() | 零 JS |
| 区块标题 | header 和 body 各一个 `<h2>`（重复） | 只保留一个 `<h2>`，紫色 |
| 代码量 | — | 净减 829 行 |

---

## v5 — 白色背景
**文件：** `v5_white_bg.html`
**提交：** `efb57b5` — 切换为白色背景主题

### 设计变更
| 元素 | v4 (暗) → v5 (白) |
|------|-------------------|
| 页面背景 | `#201d1d` → `#ffffff` |
| 区块底色 | `#302c2c` → `#f8f7f7` |
| 标题文字 | `#fdfcfc` → `#1d1d1f` |
| 正文 | `#d6d3d3` → `#424245` |
| 分割线 | 白半透明 → 黑半透明 |
| 行内代码 | 暗底白字 → 紫色文字+紫色浅底 |
| 标签色 | 荧光绿/橙/红 → 深绿/深橙/深红 |
| 强调 hover | `#a78bfa` → `#6d28d9` |

### 不变
- 紫色主色 `#7c3aed`
- 导航栏、Hero 结构
- 全部 17 章节内容

---

## v6 — 字标移到导航栏
**文件：** `v6_wordmark_nav.html` ← **当前版本**
**提交：** `fc3e08a` — ROCKY ASCII字标移到导航栏左上角

### 设计变更
| 维度 | v5 → v6 |
|------|---------|
| ASCII 字标位置 | Hero 居中 → 导航栏左上角 |
| 字标尺寸 | 12px → 10px |
| 导航品牌 | 纯文字 "Rocky" → 6 行 ASCII 像素字 |
| Hero | 字标 + 标题 + 副标题 → 仅标题 + 副标题 |
| 导航布局 | — | tabs 垂直居中对齐字标 |
| 移动端 | — | 导航纵向堆叠、字标 7px |

---

## v7 — OpenCode 风格首页
**文件：** `v7_opencode_homepage.html` ← **当前版本**
**提交：** (待推送)

### 设计变更
| 维度 | v6 → v7 |
|------|---------|
| 页面类型 | 文档内页 → 产品首页 (Landing Page) |
| 顶部 | 无 → Beta 公告横幅 |
| Hero | h1 + 副标题 | 44px 大标题 + 描述 + 安装命令 + CTA |
| 安装区 | 无 → 多标签切换 (uv / pip / curl / git) + 一键复制 |
| 功能列表 | 手风琴/平铺章节 | `[*]` 标记的 2 列网格 (10 条) |
| 数据区 | 无 → 3 列统计卡片 (75+ / SWE-bench / MIT) |
| 隐私区 | 无 → 独立章节 |
| FAQ | 无 → 7 个问答 |
| 导航 | 3 个标签 | 3 个标签 + 右侧「立即下载」实心按钮 |
| 宽度 | 880px → 960px |

### 新增内容
- 公告横幅：Beta 版支持 macOS / Windows / Linux
- 安装命令多标签切换 + 复制按钮
- 功能亮点 2 列网格
- 统计数据卡片
- 隐私优先说明
- 常见问题 (7 条)

### 保留
- 紫色主色 `#7c3aed`
- 白色背景 + 等宽字体
- ASCII 字标导航栏

---

## 版本对比速览

```
v1: 浅紫手风琴 ──→ v2: 暗色终端风 ──→ v3: 紫色导航
                                        │
                                        ▼
              v6: 字标左上 → v7: OpenCode 风格首页
                                       【当前版本】
```
